## GitHub Pages でグラフが表示されない場合の対応 🔄

### 🌐 アクセスURL
```
https://yakinorinori.github.io/My-Web-site/
```

### ❌ 問題が発生した場合

#### **1. ブラウザキャッシュをクリア**

**Chrome/Edge:**
1. `F12` キーを押して開発者ツールを開く
2. `Network` タブをクリック
3. **Disable cache** チェックボックスにチェック
4. ページをリロード（`Ctrl+Shift+R` または `Cmd+Shift+R`）

**Safari:**
1. `Cmd+Option+E` で履歴を空にする
2. または `Cmd+,` → 詳細 → キャッシュを空にする
3. ページをリロード（`Cmd+R`）

#### **2. 強制リロード**
- **Windows/Linux**: `Ctrl+Shift+R`
- **Mac**: `Cmd+Shift+R`

#### **3. 別のブラウザで試す**
- Chrome, Firefox, Safari など異なるブラウザで試す

#### **4. localStorage をクリア**
ブラウザコンソール（F12）で実行：
```javascript
localStorage.clear();
location.reload();
```

### ✅ 確認事項

#### **表示される内容**
- [ ] 「📊 売上管理Webサイト - デモ版」というタイトルが見える
- [ ] 「年ごとの分析」「月ごとの分析」ボタンが見える
- [ ] **「📈 前年度比較」ボタンが見える** ← 新機能

#### **前年度比較グラフ**
1. **「📈 前年度比較」ボタンをクリック**
2. **折れ線グラフが表示される**
   - 2023年（青色）
   - 2024年（オレンジ色）
   - 2025年（赤色）
3. **月別比較テーブルが表示される**
   - 当年度 vs 前年度の比率（%）
   - 成長は🟢緑、減少は🔴赤で表示

### 🔍 ブラウザコンソールで確認

`F12` キーを押してコンソールタブで以下の内容を確認：

```javascript
// ✅ 正常なログ
✅ デモデータ読み込み完了（2820行）
✅ GitHub Pages認証済み
📥 データ取得中: demo data from ./sales.csv

// ❌ エラーログ（出ていないこと）
❌ initializeDefaultApiKey is not defined
❌ Cannot find sales.csv
```

### 📊 データの確認

コンソールで実行：
```javascript
// グローバルデータの行数を確認
console.log('データ行数:', globalData.length);
// → 2820 が表示されれば正常

// 年別データを確認
const years = Array.from(new Set(globalData.map(r => r['日付'].slice(0,4)))).sort();
console.log('年別:', years);
// → ['2023', '2024', '2025'] が表示されれば正常
```

### 🔄 キャッシュクリア後の流れ

```
1. ブラウザキャッシュクリア
   ↓
2. https://yakinorinori.github.io/My-Web-site/ にアクセス
   ↓
3. デモアプリが起動
   ↓
4. 「📈 前年度比較」ボタンをクリック
   ↓
5. 3年分の月別グラフと比較テーブルが表示
```

### 💡 トラブルシューティング

| 問題 | 対応 |
|------|------|
| グラフが表示されない | キャッシュクリア + 強制リロード |
| 前年度比較ボタンが見えない | 開発者ツール（F12）で main.js の読み込みを確認 |
| データが古い | localStorage.clear() してからリロード |
| CORS エラーが出る | 別のブラウザで試す |

### 📱 GitHub Pages でのデータソース

```
frontend/sales.csv
  ↓ 相対パス（./sales.csv）で読み込み
  ↓ 2,820行のデータ
    - 2023年6月-12月: 858行
    - 2024年1月-12月: 1,481行
    - 2025年1月-11月: 481行
  ↓
  Chart.js でグラフ生成
```

### ✨ 新機能の確認

- **前年度比較グラフ** ← NEW
  - 複数年の月別売上推移
  - 色分けされたラインで直感的に比較
- **比較テーブル**
  - 当年度 vs 前年度の月別比較
  - 売上・客数の比率（%表示）
